{
  "name": "trstudio-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check shared/content-utils.js && node --check scripts/site.js && node --check scripts/admin.js"
  }
}
